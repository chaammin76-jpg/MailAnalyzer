<Window x:Class="MailAnalyzer.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки"
        Width="650"
        Height="430"
        WindowStartupLocation="CenterOwner"
        Background="#1E2D3D">

    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="Настройки"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#F0F4F8"
                   Margin="0,0,0,25"/>

        <!-- Настройки -->
        <Grid Grid.Row="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Учитывать регистр -->
            <CheckBox x:Name="CaseSensitiveCheckBox"
                      Grid.Row="0"
                      Content="Учитывать регистр при определении уникальности"
                      Foreground="#F0F4F8"
                      FontSize="15"
                      Margin="0,0,0,20"/>

            <!-- Сортировка -->
            <CheckBox x:Name="SortResultsCheckBox"
                      Grid.Row="1"
                      Content="Сортировать список уникальных e-mail адресов"
                      Foreground="#F0F4F8"
                      FontSize="15"
                      Margin="0,0,0,20"/>

            <!-- Путь -->
            <Grid Grid.Row="2"
                  Margin="0,0,0,20">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Путь сохранения:"
                           VerticalAlignment="Center"
                           Foreground="#F0F4F8"
                           FontSize="15"/>

                <TextBox x:Name="DefaultSavePathTextBox"
                         Grid.Column="1"
                         Height="34"
                         VerticalContentAlignment="Center"
                         Background="#1B2B3B"
                         Foreground="#F0F4F8"
                         BorderBrush="#45627D"
                         Padding="8"/>

                <Button Grid.Column="2"
                        Content="Обзор..."
                        Height="34"
                        Margin="10,0,0,0"
                        Click="BrowseButton_Click"/>

            </Grid>

            <!-- Формат -->
            <Grid Grid.Row="3">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Формат результата:"
                           VerticalAlignment="Center"
                           Foreground="#F0F4F8"
                           FontSize="15"/>

				<ComboBox x:Name="DefaultFormatComboBox"
                          Grid.Column="1"
                          Height="34"
                          Foreground="#F0F4F8"
                          BorderBrush="#45627D"
                          BorderThickness="1"
                          Padding="8,0"
                          OverridesDefaultStyle="True">

					<ComboBox.Template>
						<ControlTemplate TargetType="ComboBox">

							<Grid>

								<!-- Основное поле -->
								<Border x:Name="MainBorder"
										Background="#263B50"
										BorderBrush="#45627D"
										BorderThickness="1">

									<Grid>

										<!-- Выбранное значение -->
										<ContentPresenter
											Margin="8,0,35,0"
											VerticalAlignment="Center"
											HorizontalAlignment="Left"
											Content="{TemplateBinding SelectionBoxItem}"
											ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
											TextElement.Foreground="#F0F4F8"/>

										<!-- Кнопка открытия списка -->
										<ToggleButton
											Grid.Column="1"
											Width="34"
											HorizontalAlignment="Right"
											Background="#263B50"
											BorderThickness="0"
											IsChecked="{Binding IsDropDownOpen,
                                                RelativeSource={RelativeSource TemplatedParent},
                                                Mode=TwoWay}">

											<ToggleButton.Template>
												<ControlTemplate TargetType="ToggleButton">

													<Border Background="{TemplateBinding Background}">

														<Grid>

															<Path Width="8"
																  Height="5"
																  HorizontalAlignment="Center"
																  VerticalAlignment="Center"
																  Fill="#F0F4F8"
																  Data="M 0 0 L 8 0 L 4 5 Z"/>

														</Grid>

													</Border>

												</ControlTemplate>
											</ToggleButton.Template>

										</ToggleButton>

									</Grid>

								</Border>

								<!-- Выпадающий список -->
								<Popup x:Name="PART_Popup"
									   AllowsTransparency="True"
									   Placement="Bottom"
									   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
									   IsOpen="{TemplateBinding IsDropDownOpen}"
									   PopupAnimation="Slide">

									<Border Background="#263B50"
											BorderBrush="#45627D"
											BorderThickness="1"
											MinWidth="{Binding ActualWidth,
                                               RelativeSource={RelativeSource TemplatedParent}}">

										<ScrollViewer MaxHeight="200"
              CanContentScroll="True">

											<ScrollViewer.Resources>

												<!-- Фон вертикального скроллбара -->
												<Style TargetType="ScrollBar">

													<Setter Property="Background"
															Value="#1B2B3B"/>

													<Setter Property="Foreground"
															Value="#45627D"/>

													<Setter Property="Width"
															Value="10"/>

													<Setter Property="Template">

														<Setter.Value>
															<ControlTemplate TargetType="ScrollBar">

																<Grid Background="#1B2B3B">

																	<Track x:Name="PART_Track"
																		   IsDirectionReversed="True">

																		<Track.DecreaseRepeatButton>
																			<RepeatButton
																				Command="ScrollBar.LineUpCommand"
																				Background="Transparent"
																				BorderThickness="0"/>
																		</Track.DecreaseRepeatButton>

																		<Track.Thumb>
																			<Thumb>

																				<Thumb.Template>
																					<ControlTemplate TargetType="Thumb">

																						<Border
																							Background="#45627D"
																							CornerRadius="4"
																							Margin="1"/>

																					</ControlTemplate>
																				</Thumb.Template>

																			</Thumb>
																		</Track.Thumb>

																		<Track.IncreaseRepeatButton>
																			<RepeatButton
																				Command="ScrollBar.LineDownCommand"
																				Background="Transparent"
																				BorderThickness="0"/>
																		</Track.IncreaseRepeatButton>

																	</Track>

																</Grid>

															</ControlTemplate>
														</Setter.Value>

													</Setter>

													<Style.Triggers>

														<Trigger Property="IsMouseOver"
																 Value="True">

															<Setter Property="Foreground"
																	Value="#3D7BD9"/>

														</Trigger>

													</Style.Triggers>

												</Style>

											</ScrollViewer.Resources>

											<ItemsPresenter/>

										</ScrollViewer>

									</Border>

								</Popup>

							</Grid>

							<ControlTemplate.Triggers>

								<Trigger Property="IsMouseOver"
										 Value="True">

									<Setter TargetName="MainBorder"
											Property="BorderBrush"
											Value="#3D7BD9"/>

								</Trigger>

								<Trigger Property="IsKeyboardFocusWithin"
										 Value="True">

									<Setter TargetName="MainBorder"
											Property="BorderBrush"
											Value="#3D7BD9"/>

								</Trigger>

							</ControlTemplate.Triggers>

						</ControlTemplate>
					</ComboBox.Template>

					<ComboBox.ItemContainerStyle>

						<Style TargetType="ComboBoxItem">

							<Setter Property="Background"
									Value="#263B50"/>

							<Setter Property="Foreground"
									Value="#F0F4F8"/>

							<Setter Property="Padding"
									Value="8,6"/>

							<Setter Property="HorizontalContentAlignment"
									Value="Left"/>

							<Style.Triggers>

								<Trigger Property="IsMouseOver"
										 Value="True">

									<Setter Property="Background"
											Value="#304D68"/>

									<Setter Property="Foreground"
											Value="#FFFFFF"/>

								</Trigger>

								<Trigger Property="IsSelected"
										 Value="True">

									<Setter Property="Background"
											Value="#3D7BD9"/>

									<Setter Property="Foreground"
											Value="#FFFFFF"/>

								</Trigger>

							</Style.Triggers>

						</Style>

					</ComboBox.ItemContainerStyle>

					<ComboBoxItem Content=".txt"/>
					<ComboBoxItem Content=".csv"/>

				</ComboBox>

            </Grid>

        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">

            <Button Content="Сбросить"
                    Width="120"
                    Height="36"
                    Margin="0,0,10,0"
                    Click="ResetButton_Click"/>

            <Button Content="Отмена"
                    Width="100"
                    Height="36"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>

            <Button Content="Сохранить"
                    Width="120"
                    Height="36"
                    Click="SaveButton_Click"/>

        </StackPanel>

    </Grid>

</Window>